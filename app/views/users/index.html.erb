<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>
<p>Subdomain: <%= request.subdomain.inspect %></p>
<p>Organization: <%= @organization.inspect %>
<h1>Users:</h1>
<% if @users.empty? %>
  No Users... Subdomain is <%= request.subdomain.inspect %> which cooresponds to Organization: <%= @organization.inspect %>
<% else %>
  <% @users.each do |user| %>
    <h2><%= link_to user.name, user_path(user) %> - email: <%= user.email %> - organization: <%= user.organization.name %></h2>
  <% end %>
<% end %>